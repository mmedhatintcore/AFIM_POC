providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22", "pnpm-10_x"]

[phases.install]
cmds = [
  "npm install -g corepack@0.24.1",
  "corepack enable",
]

[phases.build]
dependsOn = ["install"]
cmds = [
  "pnpm install --frozen-lockfile",
  "pnpm run build",
]

[start]
cmd = "pnpm run start"
